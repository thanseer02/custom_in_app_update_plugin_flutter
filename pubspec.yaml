name: custom_app_update
description: UI-independent Google Play flexible updates for Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  in_app_update: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
